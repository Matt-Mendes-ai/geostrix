# TASKS.csv #363 — pinned exactly (was >=): a new major of any of these could change the numbers the
# sidecar returns with no code change. The full tree, transitive dependencies included, is in
# constraints.txt; install with  pip install -r requirements.txt -c constraints.txt
fastapi==0.141.1
uvicorn[standard]==0.52.3
numpy==2.4.6
scipy==1.17.1
pydantic==2.13.4

# TASKS.csv #29 (implicit lithology modelling): as of gempy 2026.0.3 this is a genuinely light
# add — the engine's default backend is pure numpy (no pytensor/aesara compile step), verified by
# actually installing + running an end-to-end model in the dev sandbox before wiring the endpoint.
gempy==2026.0.3
gempy_engine==2026.0.3.post1

# TASKS.csv #321 — SimPEG potential-field modelling (MIT). Pinned exactly: the inversion's numerical
# behaviour and the gravity sign handling in app/geophys/potential.py were verified against these versions
# (tests/test_potential.py), and the security review asked for exact pins on the new dependency tree.
# choclo pulls in numba/llvmlite (fast potential-field kernels); tested frozen with PyInstaller --onedir.
simpeg==0.25.2
discretize==0.12.0
geoana==0.8.1
choclo==0.3.2
pymatsolver==0.4.0
libdlf==0.3.0
